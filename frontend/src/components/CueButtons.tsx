/**
 * CueButtons — manual sound cues for the ACTIVE personality.
 *
 * Entrance theme, laugh track, etc. These fire pre-loaded SFX instantly through
 * the AudioManager (never generated live). Only the active personality's cues
 * are shown/available.
 *
 * TODO: implement. This is a scaffold stub.
 */

// import { View, Button } from 'react-native';
// import { AudioManager } from '../audio/AudioManager';

// type Props = { personalitySlug: string };

export default function CueButtons(/* props: Props */) {
  // TODO: read available cues for the active personality (assets/sounds/<slug>/)
  // TODO: render a button per cue (entrance, laugh_track, ...)
  // TODO: onPress -> AudioManager.fireCue(cueName)  (instant, pre-loaded)
  throw new Error('TODO: CueButtons');
}
