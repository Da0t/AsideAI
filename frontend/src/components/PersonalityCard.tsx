/**
 * PersonalityCard — one selectable personality in the switcher.
 *
 * Shows the personality's name/vibe; tapping makes it active.
 *
 * TODO: implement. This is a scaffold stub.
 */

// import { Pressable, Text } from 'react-native';

// type Props = { name: string; slug: string; active: boolean; onSelect: (slug: string) => void };

export default function PersonalityCard(/* props: Props */) {
  // TODO: render name + active state; onPress -> props.onSelect(props.slug)
  throw new Error('TODO: PersonalityCard');
}
