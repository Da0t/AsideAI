/**
 * ModeSwitcher — toggle the active mode.
 *
 * Mode is orthogonal to personality (e.g. how often / how punchy narration is).
 * Final mode list is a build-time decision (see docs/PERSONALITIES.md).
 *
 * TODO: implement. This is a scaffold stub.
 */

// import { View } from 'react-native';

// type Props = { mode: string; onChange: (mode: string) => void };

export default function ModeSwitcher(/* props: Props */) {
  // TODO: render mode options; onChange -> props.onChange(mode)
  throw new Error('TODO: ModeSwitcher');
}
