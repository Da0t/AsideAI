/**
 * CustomPersonalityBuilderScreen.
 *
 * Make a new personality the easy way: pick a Deepgram voice and describe the
 * character in free text. Produces a normal bundle stored alongside the
 * built-ins. Everything is free — no gating.
 *
 * See ../../docs/PERSONALITIES.md "Custom Personalities".
 *
 * TODO: implement. This is a scaffold stub.
 */

// import { View, TextInput } from 'react-native';
// import * as backend from '../api/backend';

export default function CustomPersonalityBuilderScreen() {
  // TODO: voice picker -> deepgram_voice (list of available Deepgram TTS voices)
  // TODO: free-text field -> character description (seeds claude_system_prompt)
  // TODO: name field -> name + derived slug
  // TODO: default sound_cues (shared/neutral set — custom personalities don't ship audio)
  // TODO: save -> backend.createCustomPersonality(bundle)
  throw new Error('TODO: CustomPersonalityBuilderScreen');
}
